@model IEnumerable<ExpenseManager.Models.Category>

@{
    ViewBag.Title = "Index";
}


<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th></th>
    </tr>

 
@foreach (var c in Model.Where(c=> c.User.UserName == User.Identity.Name)) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => c.Title)
        </td>
        <td>
           Avtor: @Html.DisplayFor(modelItem => c.User.UserName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=c.Id }) |
            @Html.ActionLink("Details", "Details", new { id=c.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=c.Id })
        </td>
    </tr>
}

</table>
